import {useTheme} from '../../app/providers/ThemeProvider'
export function ThemeToggle({label='Giao diện'}){const theme=useTheme();if(!theme)return null;return <label className="theme-toggle"><span>{label}</span><select aria-label="Giao diện" value={theme.preference} onChange={event=>theme.setPreference(event.target.value)}><option value="light">Sáng</option><option value="dark">Tối</option><option value="system">Theo hệ thống</option></select></label>}
