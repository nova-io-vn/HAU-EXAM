export function Input({label,error,helper,id,...props}){const inputId=id||props.name;return <label className="field" htmlFor={inputId}><span>{label}</span><input id={inputId} aria-invalid={Boolean(error)} aria-describedby={error||helper?`${inputId}-help`:undefined} {...props}/>{(error||helper)&&<small id={`${inputId}-help`} className={error?'field-error':''}>{error||helper}</small>}</label>}
