import {useCallback,useEffect,useState} from 'react'
import {Link,useParams} from 'react-router-dom'
import {Button,ConfirmDialog,Dialog,Input,Select,StatusBadge} from '../../../components/ui'
import {PageHeader} from '../../../components/shared/PageHeader'
import {routes} from '../../../constants/routes'
import {usersApi} from '../api/usersApi'
import {RequestState} from '../components/RequestState'
import {formatDateTime,userRoles} from '../model/userModel'

export function UserDetailPage(){const{id}=useParams();const[user,setUser]=useState(null);const[loading,setLoading]=useState(true);const[error,setError]=useState(null);const[dialog,setDialog]=useState(null);const[value,setValue]=useState('');const[confirmation,setConfirmation]=useState(null);const load=useCallback(async()=>{setLoading(true);setError(null);try{setUser(await usersApi.get(id))}catch(reason){setError(reason)}finally{setLoading(false)}},[id]);useEffect(()=>{const task=setTimeout(load,0);return()=>clearTimeout(task)},[load]);async function saveAssignment(){try{if(dialog==='role')await usersApi.assignRole(id,value);else await usersApi.assignFaculty(id,value.trim());setDialog(null);await load()}catch(reason){setError(reason)}}async function confirmAction(){const action=confirmation;setConfirmation(null);try{await usersApi[action](id);await load()}catch(reason){setError(reason)}}if(loading||error)return <section><PageHeader title="Chi tiết người dùng"/><div className="surface"><RequestState loading={loading} error={error} onRetry={load}/></div></section>;return <section><PageHeader title={user.fullName||user.lecturerCode} description="Thông tin hồ sơ và thao tác quản trị tài khoản." actions={<Link to={routes.users}><Button variant="secondary">Quay lại</Button></Link>}/><div className="detail-grid"><article className="surface detail-card"><header><div className="profile-avatar">{user.avatar?<img src={user.avatar} alt=""/>:(user.fullName||user.lecturerCode).slice(0,2).toUpperCase()}</div><div><h2>{user.fullName||'Chưa cập nhật họ tên'}</h2><p>{user.lecturerCode}</p></div><StatusBadge status={user.status}/></header><dl><Info label="Email" value={user.email}/><Info label="Điện thoại" value={user.phone}/><Info label="Ngày sinh" value={user.dateOfBirth}/><Info label="Địa chỉ" value={user.address}/><Info label="Khoa" value={user.facultyId}/><Info label="Vai trò" value={user.role}/><Info label="Cập nhật" value={formatDateTime(user.updatedAt)}/></dl></article><aside className="surface admin-actions"><h2>Quản trị tài khoản</h2><p>Các thay đổi vẫn được User Service kiểm tra quyền và business rule.</p><Button variant="secondary" onClick={()=>{setValue(user.role);setDialog('role')}}>Gán vai trò</Button><Button variant="secondary" onClick={()=>{setValue(user.facultyId||'');setDialog('faculty')}}>Gán hoặc chuyển khoa</Button>{user.status==='LOCKED'?<Button onClick={()=>setConfirmation('unlock')}>Mở khóa tài khoản</Button>:<Button variant="danger" onClick={()=>setConfirmation('lock')}>Khóa tài khoản</Button>}</aside></div><Dialog open={dialog==='role'} title="Gán vai trò" onClose={()=>setDialog(null)} footer={<><Button variant="secondary" onClick={()=>setDialog(null)}>Hủy</Button><Button onClick={saveAssignment}>Lưu</Button></>}><Select label="Vai trò" value={value} onChange={event=>setValue(event.target.value)} options={userRoles.map(role=>({value:role,label:role}))}/><p className="form-note">SUBJECT_ADMIN cần có khoa rõ ràng; backend quyết định tính hợp lệ cuối cùng.</p></Dialog><Dialog open={dialog==='faculty'} title="Gán hoặc chuyển khoa" onClose={()=>setDialog(null)} footer={<><Button variant="secondary" onClick={()=>setDialog(null)}>Hủy</Button><Button disabled={!value.trim()} onClick={saveAssignment}>Lưu</Button></>}><Input label="Faculty ID" value={value} onChange={event=>setValue(event.target.value)} required/><p className="form-note">Danh mục khoa chưa có API được công bố nên không tự tạo dữ liệu lựa chọn ở frontend.</p></Dialog><ConfirmDialog open={Boolean(confirmation)} danger={confirmation==='lock'} title={confirmation==='lock'?'Khóa tài khoản?':'Mở khóa tài khoản?'} description={confirmation==='lock'?`Tài khoản ${user.lecturerCode} sẽ không thể tiếp tục hoạt động.`:`Tài khoản ${user.lecturerCode} sẽ được mở khóa ở góc độ nghiệp vụ.`} confirmLabel={confirmation==='lock'?'Khóa tài khoản':'Mở khóa'} onClose={()=>setConfirmation(null)} onConfirm={confirmAction}/></section>}
function Info({label,value}){return <div><dt>{label}</dt><dd>{value||'—'}</dd></div>}
