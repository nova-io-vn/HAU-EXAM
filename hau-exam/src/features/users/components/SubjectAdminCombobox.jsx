import {useMemo,useRef,useState,useEffect} from 'react'
export function SubjectAdminCombobox({users,value,onChange}){const root=useRef(null);const[open,setOpen]=useState(false);const[query,setQuery]=useState('');useEffect(()=>{const close=e=>{if(!root.current?.contains(e.target))setOpen(false)};document.addEventListener('pointerdown',close);return()=>document.removeEventListener('pointerdown',close)},[]);const filtered=useMemo(()=>{const q=query.trim().toLocaleLowerCase('vi-VN');return users.filter(u=>!q||`${u.fullName} ${u.lecturerCode}`.toLocaleLowerCase('vi-VN').includes(q))},[users,query]);const selected=users.find(u=>u.id===value);return <div className="subject-admin-combobox field" ref={root}><span>Quản trị chuyên môn</span><button type="button" className="faculty-combobox-trigger" onClick={()=>setOpen(v=>!v)} aria-haspopup="listbox" aria-expanded={open}>{selected?<><strong>{selected.fullName}</strong><small>{selected.lecturerCode} · {selected.facultyName||'Chưa phân khoa'}</small></>:<span>Chưa phân công</span>}</button>{open&&<div className="faculty-combobox-popover"><input autoFocus value={query} onChange={e=>setQuery(e.target.value)} placeholder="Tìm kiếm giảng viên..." aria-label="Tìm kiếm giảng viên"/><div role="listbox"><button type="button" role="option" onClick={()=>{onChange('');setOpen(false)}}><strong>Chưa phân công</strong></button>{filtered.map(u=><button type="button" role="option" key={u.id} onClick={()=>{onChange(u.id);setOpen(false);setQuery('')}}><strong>{u.fullName}</strong><small>{u.lecturerCode} · {u.facultyName||u.facultyId||'Chưa phân khoa'} · {u.role}</small></button>)}</div></div>}</div>}
