import {useCallback,useEffect,useRef,useState} from 'react'
import {Link} from 'react-router-dom'
import {Button,ConfirmDialog,Drawer,Loading} from '../../../components/ui'
import {PageHeader} from '../../../components/shared/PageHeader'
import {routes} from '../../../constants/routes'
import {roles} from '../../../constants/roles'
import {useAuth} from '../../auth/hooks/useAuth'
import {questionsApi} from '../api/questionsApi'
import {QuestionFilters} from '../components/QuestionFilters'
import {QuestionPagination} from '../components/QuestionPagination'
import {QuestionPreview} from '../components/QuestionPreview'
import {QuestionTable} from '../components/QuestionTable'
import {useQuestionCatalogs} from '../hooks/useQuestionCatalogs'
import {normalizePage} from '../model/questionModel'

const initialFilters={facultyId:'',subjectId:'',chapterId:'',topicId:'',difficulty:'',status:'',source:'',keyword:''}
export function QuestionListPage(){const auth=useAuth();const requestVersion=useRef(0);const isOwnerView=auth.role===roles.USER;const creatorId=auth.currentUser?.id;const[draft,setDraft]=useState(()=>({...initialFilters,facultyId:auth.facultyId||''}));const[query,setQuery]=useState(()=>({...initialFilters,facultyId:auth.facultyId||''}));const[page,setPage]=useState(0);const[data,setData]=useState(normalizePage());const[loading,setLoading]=useState(true);const[busy,setBusy]=useState(false);const[error,setError]=useState(null);const[preview,setPreview]=useState(null);const[confirmation,setConfirmation]=useState(null);const catalogs=useQuestionCatalogs(draft.subjectId,draft.chapterId);const load=useCallback(async()=>{const version=++requestVersion.current;setLoading(true);setError(null);try{const result=await questionsApi.list({...query,createdBy:isOwnerView?creatorId:undefined,page,size:10});if(version===requestVersion.current)setData(normalizePage(result))}catch(reason){if(version===requestVersion.current)setError(reason)}finally{if(version===requestVersion.current)setLoading(false)}},[creatorId,isOwnerView,page,query]);useEffect(()=>{const task=setTimeout(load,0);return()=>clearTimeout(task)},[load]);async function confirmAction(){if(busy)return;const{action,question}=confirmation;setBusy(true);try{await questionsApi[action](question.id);setConfirmation(null);await load()}catch(reason){setConfirmation(null);setError(reason)}finally{setBusy(false)}}return <section><PageHeader title={isOwnerView?'Câu hỏi của tôi':'Ngân hàng câu hỏi'} description={isOwnerView?'Theo dõi, chỉnh sửa và gửi duyệt câu hỏi do bạn tạo.':'Tra cứu ngân hàng câu hỏi trong phạm vi khoa được cấp.'} actions={isOwnerView&&<Link to={routes.newQuestion}><Button>Tạo câu hỏi</Button></Link>}/><QuestionFilters filters={draft} facultyLocked={Boolean(auth.facultyId)} catalogs={catalogs} onChange={setDraft} onSubmit={event=>{event.preventDefault();setPage(0);setQuery(draft)}}/><div className="surface question-table-surface">{loading?<div className="question-state"><Loading label="Đang tải câu hỏi"/></div>:error?<div className="question-state request-error" role="alert"><strong>Không thể tải câu hỏi</strong><span>{error.status===403?'Bạn không có quyền truy cập câu hỏi trong phạm vi này.':error.status===401?'Phiên đăng nhập đã hết hạn. Vui lòng đăng nhập lại.':error.message}</span>{error.correlationId&&<small>Mã hỗ trợ: {error.correlationId}</small>}<Button variant="secondary" onClick={load}>Thử lại</Button></div>:<><QuestionTable questions={data.items} auth={auth} onPreview={setPreview} onAction={(action,question)=>setConfirmation({action,question})}/><QuestionPagination {...data} onChange={setPage}/></>}</div><Drawer open={Boolean(preview)} title="Xem trước câu hỏi" onClose={()=>setPreview(null)}><QuestionPreview question={preview}/></Drawer><ConfirmDialog loading={busy} open={Boolean(confirmation)} danger={confirmation?.action==='archive'} title={confirmation?.action==='archive'?'Lưu trữ câu hỏi?':'Gửi câu hỏi xét duyệt?'} description={confirmation?.action==='archive'?'Câu hỏi sẽ được chuyển qua action archive của Question Service.':'Question Service sẽ kiểm tra workflow trước khi chuyển sang chờ duyệt.'} confirmLabel={confirmation?.action==='archive'?'Lưu trữ':'Gửi duyệt'} onClose={()=>{if(!busy)setConfirmation(null)}} onConfirm={confirmAction}/></section>}
