export function normalizeNotification(message){const source=message?.payload||message||{};return{id:source.id||source.notificationId||message?.eventId,eventId:message?.eventId||source.eventId,type:source.type||message?.eventType||'SYSTEM',title:source.title||'Thông báo mới',content:source.content||source.message||'',referenceId:source.referenceId||null,referenceType:source.referenceType||null,isRead:Boolean(source.isRead),createdAt:source.createdAt||message?.occurredAt||new Date().toISOString()}}
export function normalizeNotificationPage(result){if(Array.isArray(result))return{items:result.map(normalizeNotification),page:0,totalPages:1,totalElements:result.length};return{items:(result?.content||result?.items||[]).map(normalizeNotification),page:result?.number??result?.page??0,totalPages:result?.totalPages??0,totalElements:result?.totalElements??0}}
export function notificationTarget(notification){if(!notification.referenceId)return null;const type=String(notification.referenceType||'').toUpperCase();if(type==='QUESTION')return`/questions/${notification.referenceId}`;if(type==='AI_JOB'||type==='AIJOB')return`/ai/jobs?jobId=${notification.referenceId}`;if(type==='EXAM')return`/exams?examId=${notification.referenceId}`;if(type==='USER'||type==='USER_PROFILE')return`/admin/users/${notification.referenceId}`;return null}
export function formatNotificationTime(value){if(!value)return'';const date=new Date(value);return Number.isNaN(date.getTime())?'':new Intl.DateTimeFormat('vi-VN',{dateStyle:'short',timeStyle:'short'}).format(date)}
