import {useNavigate} from 'react-router-dom'
import {notificationStore} from '../store/notificationStore'
import {formatNotificationTime,notificationTarget} from '../model/notificationModel'

export function NotificationItem({notification,compact=false,onSelected,onRead}){const navigate=useNavigate();const target=notificationTarget(notification);async function select(){try{if(!notification.isRead){await notificationStore.markRead(notification.id);onRead?.(notification)}}finally{onSelected?.();if(target)navigate(target)}}return <article className={`notification-item ${notification.isRead?'is-read':'is-unread'} ${compact?'is-compact':''}`}><button type="button" onClick={select} aria-label={`${notification.isRead?'Đã đọc':'Chưa đọc'}: ${notification.title}`}><span className="notification-dot" aria-hidden="true"/><span className="notification-copy"><span className="notification-meta"><strong>{notification.type.replaceAll('_',' ')}</strong><time dateTime={notification.createdAt}>{formatNotificationTime(notification.createdAt)}</time></span><b>{notification.title}</b><span>{notification.content}</span>{target&&<small>Mở nội dung liên quan →</small>}</span></button></article>}
