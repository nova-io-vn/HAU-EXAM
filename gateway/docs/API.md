> **Vị trí đặt file:** `backend/api-gateway/docs/API.md`

# API Gateway --- Routes

Expose `/api/v1/auth/**`, `/users/**`, `/questions/**`, `/subjects/**`,
`/chapters/**`, `/topics/**`, `/exams/**`, `/exam-matrices/**`,
`/ai/**`, `/documents/**`, `/chat/**`, `/notifications/**` và WebSocket
route theo cấu hình thực tế. Route phải khớp service contract.
