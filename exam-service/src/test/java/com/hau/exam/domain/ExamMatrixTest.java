package com.hau.exam.domain;import static org.junit.jupiter.api.Assertions.*;import com.hau.exam.domain.exception.InvalidMatrixException;import com.hau.exam.domain.model.*;import java.time.Instant;import java.util.*;import org.junit.jupiter.api.Test;class ExamMatrixTest{@Test void distributionMustMatchTotal(){var r=new ExamMatrixRule(UUID.randomUUID(),UUID.randomUUID(),null,Difficulty.EASY,2);assertThrows(InvalidMatrixException.class,()->new ExamMatrix(UUID.randomUUID(),"M","CNTT",UUID.randomUUID(),3,List.of(r),UUID.randomUUID(),Instant.now(),Instant.now()));}@Test void negativeCountRejected(){assertThrows(IllegalArgumentException.class,()->new ExamMatrixRule(UUID.randomUUID(),UUID.randomUUID(),null,Difficulty.EASY,-1));}}
