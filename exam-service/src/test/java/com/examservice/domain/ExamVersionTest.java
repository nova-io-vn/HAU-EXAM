package com.examservice.domain;import static org.junit.jupiter.api.Assertions.*;import com.examservice.domain.model.*;import java.time.Instant;import java.util.*;import org.junit.jupiter.api.Test;class ExamVersionTest{@Test void duplicateQuestionRejected(){UUID q=UUID.randomUUID(),rule=UUID.randomUUID();var refs=List.of(new ExamQuestionReference(UUID.randomUUID(),q,1,rule),new ExamQuestionReference(UUID.randomUUID(),q,2,rule));assertThrows(IllegalArgumentException.class,()->new ExamVersion(UUID.randomUUID(),1,refs,Instant.now()));}}
