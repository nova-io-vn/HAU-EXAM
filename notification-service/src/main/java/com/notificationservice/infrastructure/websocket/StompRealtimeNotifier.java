package com.notificationservice.infrastructure.websocket;import com.notificationservice.application.port.out.RealtimeNotifier;import com.notificationservice.domain.model.Notification;import com.notificationservice.presentation.response.NotificationResponse;import org.springframework.messaging.simp.SimpMessagingTemplate;import org.springframework.stereotype.Component;
@Component public class StompRealtimeNotifier implements RealtimeNotifier{private final SimpMessagingTemplate template;public StompRealtimeNotifier(SimpMessagingTemplate t){template=t;}public void send(Notification n){template.convertAndSendToUser(n.getUserId().toString(),"/queue/notifications",NotificationResponse.from(n));}}
