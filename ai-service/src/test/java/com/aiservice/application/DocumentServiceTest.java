package com.aiservice.application;import static org.junit.jupiter.api.Assertions.*;import static org.mockito.Mockito.*;import com.aiservice.application.port.out.*;import com.aiservice.application.service.DocumentService;import com.aiservice.domain.exception.UnsupportedDocumentException;import java.io.*;import java.time.Clock;import java.util.UUID;import org.junit.jupiter.api.Test;import org.springframework.mock.env.MockEnvironment;class DocumentServiceTest{@Test void unsupportedTypeRejected(){var s=new DocumentService(mock(StoragePort.class),mock(DocumentRepository.class),Clock.systemUTC(),new MockEnvironment());assertThrows(UnsupportedDocumentException.class,()->s.upload(UUID.randomUUID(),"x.pdf","application/pdf",10,new ByteArrayInputStream(new byte[10])));}}
