Bạn đang thực hiện một FINAL WEB UI / ROUTING POLISH PASS
cho HAU-EXAM / HAU QM System.

Đây là task WEB FRONTEND ONLY.

==================================================
0. ABSOLUTE RULES
==================================================

TUYỆT ĐỐI KHÔNG sửa Backend.

Không sửa:

- auth-service
- user-service
- question-service
- exam-service
- ai-service
- notification-service
- gateway
- eureka
- RabbitMQ
- PostgreSQL
- Flyway
- Docker / docker-compose

Không thay API contract.

Không tạo endpoint mới.

Không sửa Mobile.

Không sửa .env secrets.

Không Docker build/restart.

Không commit.
Không push.

Chỉ được sửa Web frontend trong:

hau-exam/

Nếu phát hiện Backend permission/API không phù hợp:
KHÔNG sửa Backend.

Ghi lại:

BACKEND_GAP_NOT_CHANGED

trong final report.

==================================================
1. VẤN ĐỀ HIỆN TẠI
==================================================

Từ UI hiện tại phát hiện:

1. USER/Giảng viên đang nhìn thấy:
   - Ma trận đề
   - Phiên bản đề

2. Khi truy cập một số route này có thể nhận:

403
“Không có quyền truy cập”

Điều này tạo UX sai:

UI cho phép user nhìn thấy navigation item
nhưng route/permission lại không cho truy cập.

3. Hiện đã có 403 page nhưng cần chuẩn hóa:

- 401
- 403
- 404
- 500
- generic unexpected error

4. Web còn khá tĩnh.

Cần thêm hover/transition nhẹ nhưng:

- không màu mè
- không gradient
- không animation lớn
- không scale mạnh
- không glassmorphism
- không glowing
- không floating card

5. Header/Profile dropdown cần polish lại.

==================================================
2. AUDIT ROLE NAVIGATION TRƯỚC
==================================================

Trước khi sửa menu, audit:

- navigationByRole / navigation config
- route definitions
- ProtectedRoute / RoleGuard
- route permission metadata
- existing API usage
- existing E2E expectations

Mục tiêu:

MENU
        ↓
ROUTE
        ↓
FRONTEND ROLE GUARD
        ↓
BACKEND PERMISSION

phải thống nhất.

Không được có trường hợp:

menu visible
→ click
→ frontend biết trước role không được phép
→ vẫn đưa user vào 403

Nếu user không có quyền:
menu item không nên xuất hiện ngay từ đầu.

==================================================
3. USER / GIẢNG VIÊN NAVIGATION
==================================================

Audit đặc biệt:

“Ma trận đề”
“Phiên bản đề”

Hiện chúng đang xuất hiện trong USER sidebar.

Theo nghiệp vụ hiện tại:

USER chủ yếu:

- Tổng quan
- Câu hỏi của tôi
- Tạo câu hỏi
- Ngân hàng câu hỏi
- Tạo câu hỏi bằng AI
- Tài liệu của tôi
- Thông báo
- Hồ sơ
- Trung tâm trợ giúp

USER không phải role quản lý chuyên môn.

Nếu frontend/backend permission hiện tại xác nhận USER
không có quyền quản lý Exam Matrix/Exam Versions:

REMOVE khỏi USER navigation.

KHÔNG xóa backend route.

KHÔNG xóa component nếu role khác vẫn sử dụng.

Chỉ sửa role navigation visibility.

==================================================
4. SUBJECT_ADMIN EXAM NAVIGATION
==================================================

Audit quyền hiện tại.

Nếu Exam Matrix / Exam Versions thuộc SUBJECT_ADMIN:

đặt dưới group:

ĐỀ THI

- Ma trận đề
- Phiên bản đề

hoặc terminology hiện tại của Web.

SUBJECT_ADMIN chỉ thao tác trong Faculty scope
theo Backend.

Frontend không bypass faculty scope.

==================================================
5. SYSTEM_ADMIN
==================================================

Không tự động đưa Ma trận đề sang SYSTEM_ADMIN
chỉ vì SYSTEM_ADMIN là role cao nhất.

SYSTEM_ADMIN là quản trị hệ thống.

SUBJECT_ADMIN là quản trị nội dung/chuyên môn.

Audit permission hiện tại trước.

Navigation phải phản ánh business ownership,
không dựa đơn giản vào:

SYSTEM_ADMIN > SUBJECT_ADMIN > USER.

==================================================
6. ROUTE PERMISSION MATRIX
==================================================

Tạo/chuẩn hóa frontend route permission mapping.

Ví dụ concept:

route
allowedRoles

/dashboard
SYSTEM_ADMIN
SUBJECT_ADMIN
USER

/my-questions
USER

/questions/create
USER

/review
SUBJECT_ADMIN

/faculties
SYSTEM_ADMIN

/users
SYSTEM_ADMIN

/pending-users
SYSTEM_ADMIN

/exam-matrix
<theo permission thực tế sau audit>

/exam-versions
<theo permission thực tế sau audit>

Không hard-code role checks rải rác khắp component nếu project
đã có centralized config.

Ưu tiên một source of truth.

==================================================
7. 401 PAGE
==================================================

Nếu session không tồn tại hoặc hết hạn mà refresh thất bại:

ưu tiên redirect Login theo auth flow hiện tại.

Nếu cần dedicated state/page:

401

“Phiên đăng nhập đã hết hạn”

“Vui lòng đăng nhập lại để tiếp tục.”

CTA:

[Đăng nhập lại]

Không hiển thị stack trace.

==================================================
8. 403 PAGE
==================================================

Giữ design hiện tại nhưng polish.

Content:

403

“Không có quyền truy cập”

“Bạn không có quyền truy cập nội dung này.”

Không cần câu kỹ thuật kiểu:

“Việc phân quyền cuối cùng luôn được backend xác thực.”

Đó là implementation detail,
không cần nói với người dùng cuối.

CTA:

[Về trang tổng quan]

Nếu có history hợp lệ:

secondary:
[Quay lại]

Design:
centered
compact
không giant card.

==================================================
9. 404 PAGE
==================================================

Thêm dedicated 404 page.

Content:

404

“Không tìm thấy trang”

“Trang bạn đang tìm kiếm không tồn tại hoặc đã được di chuyển.”

CTA primary:

[Về trang tổng quan]

CTA secondary:

[Quay lại]

Có thể dùng icon:

document/search/compass

nhưng rất nhẹ.

Không dùng illustration hoạt hình lớn.

==================================================
10. 500 PAGE
==================================================

Thêm dedicated 500 page.

Content:

500

“Đã xảy ra lỗi”

“Hệ thống gặp sự cố khi xử lý yêu cầu. Vui lòng thử lại sau.”

Actions:

[Thử lại]

[Về trang tổng quan]

Nếu frontend có correlationId từ error:

có thể hiển thị nhỏ:

Mã tham chiếu:
<correlationId>

để debug/support.

Không show:

stacktrace
SQL
Java exception
API secret
internal URL

==================================================
11. GENERIC ERROR PAGE
==================================================

Ngoài 500, cần generic error fallback.

Ví dụ:

“Không thể tải nội dung”

“Đã xảy ra lỗi không mong muốn.”

[Thử lại]

Không biến mọi API error thành full-screen 500.

Quy tắc:

Page-level catastrophic failure
→ Error Page

Section/list API failure
→ inline ErrorState

Form validation
→ field/form error

401
→ auth handling

403
→ Forbidden Page

404 route
→ Not Found Page

==================================================
12. REACT ERROR BOUNDARY
==================================================

Audit project hiện tại.

Nếu chưa có Error Boundary:

thêm global React Error Boundary phù hợp architecture.

Nếu component render crash:

→ hiển thị friendly generic error page.

Không show React stack trong production UI.

Không swallow error silently.

==================================================
13. ROUTER CATCH-ALL
==================================================

Audit React Router hiện tại.

Unknown route phải render 404.

Ví dụ concept:

path="*"
→ NotFoundPage

Không redirect silent về dashboard.

User phải biết URL không tồn tại.

==================================================
14. API 404 KHÁC ROUTE 404
==================================================

Phân biệt:

Unknown frontend URL
→ 404 Page

Resource API 404:
ví dụ question không tồn tại
→ Resource Not Found state/page phù hợp.

Không nhất thiết tất cả API 404 đều đưa về global 404.

==================================================
15. ERROR DESIGN SYSTEM
==================================================

Tạo/reuse shared:

ErrorPage

Props concept:

code
title
description
primaryAction
secondaryAction
correlationId
icon

Reuse cho:

403
404
500

Không copy-paste ba page gần giống nhau.

==================================================
16. VISUAL STYLE ERROR PAGE
==================================================

Theo HAU design hiện tại.

Background:
warm off-white.

Code:

403 / 404 / 500

small uppercase/burgundy accent.

Title:
large near-black.

Description:
neutral gray.

Primary button:
HAU burgundy.

Không:

giant red 404
gradient
animated illustration
glow
full-page burgundy background

==================================================
17. HOVER SYSTEM
==================================================

Web hiện tại hơi tĩnh.

Thêm micro-interactions NHẸ.

Mục tiêu:

hover phải giúp user nhận biết:
“phần tử này có thể tương tác”.

Không dùng hover chỉ để trang trí.

==================================================
18. SIDEBAR HOVER
==================================================

Inactive sidebar item:

default:
transparent

hover:
very light burgundy tint

Ví dụ visual:

background rgba burgundy khoảng 4–7%

text:
slightly darker

icon:
burgundy/neutral transition

Transition:

150–180ms ease

Không translate lớn.

Có thể translateX tối đa khoảng 1–2px
hoặc không translate.

Active:

light burgundy background
burgundy text/icon
clear indicator

Active không được giống hover.

==================================================
19. BUTTON HOVER
==================================================

Primary button:

default:
HAU burgundy

hover:
darken nhẹ

active:
darken thêm rất nhẹ

Transition:
~150ms

Không:

scale 1.1
glow
gradient
bounce

Có thể:

transform translateY(-1px)

NHƯNG chỉ nếu toàn design system đang dùng.

Nếu không:
chỉ đổi background/shadow nhẹ.

==================================================
20. SECONDARY BUTTON
==================================================

Default:
white / transparent
border neutral

Hover:
light neutral / burgundy tint

Border:
burgundy nhẹ nếu phù hợp.

Không biến secondary thành primary khi hover.

==================================================
21. CARDS
==================================================

Chỉ interactive cards mới có hover.

Clickable card:

hover:
border slightly stronger
very subtle shadow
background almost unchanged

Có thể:

box-shadow:
0 2px 8px rgba(... very low opacity)

Không dùng:

large shadow
translateY(-8px)
scale
rotate

Static KPI cards:
không bắt buộc hover.

==================================================
22. TABLE / LIST ROW
==================================================

Rows clickable:

hover:
very light neutral/burgundy tint

Cursor:
pointer

Rows không clickable:
không dùng pointer.

Không làm toàn bộ table đổi màu mạnh.

==================================================
23. ICON BUTTONS
==================================================

Notification bell
Help
Menu
Actions

Hover:

light circular/square background
burgundy tint

Transition:
150ms

Touch/click area vẫn đủ lớn.

Không chỉ hover trực tiếp SVG 16px.

==================================================
24. INPUT / SEARCH
==================================================

Input default:
neutral border.

Hover:
border slightly darker.

Focus:
burgundy border
subtle focus ring.

Ví dụ:

focus ring burgundy ~10–15% opacity.

Không dùng bright blue browser-like ring nếu design system đã override.

Nhưng vẫn phải giữ accessibility focus-visible.

==================================================
25. DROPDOWN
==================================================

User dropdown hiện tại cần polish.

Vấn đề screenshot:

Header đã hiển thị:

M123456
Giảng viên • Khoa ...

Dropdown lại mở đầu bằng:

M123456
Giảng viên • Khoa ...

→ tạo cảm giác lặp identity.

Sửa theo hướng:

HEADER:

avatar
fullName hoặc lecturerCode
role • faculty
chevron

DROPDOWN:

không cần lặp nguyên identity block lớn lần nữa.

Có thể dùng compact header:

Hồ sơ tài khoản

sau đó:

Hồ sơ cá nhân
Trung tâm trợ giúp

divider

Đăng xuất

HOẶC nếu cần identity trong dropdown:
chỉ render rất compact,
không lặp lecturerCode lớn.

==================================================
26. USER IDENTITY
==================================================

Ưu tiên:

fullName

Nếu fullName tồn tại:

Nguyễn Văn A
Giảng viên • Khoa Công nghệ thông tin

Nếu không:

M123456
Giảng viên • Khoa Công nghệ thông tin

Không render:

M123456
...
M123456

trong cùng một visual region.

==================================================
27. USER DROPDOWN HOVER
==================================================

Menu items:

Hồ sơ cá nhân
Trung tâm trợ giúp

Hover:
light neutral/burgundy background.

Logout:

text semantic red.

Hover:
very light red background.

Không để logout text quá lớn như CTA chính.

Dropdown width:
compact.

Padding:
gọn.

==================================================
28. NOTIFICATION BELL
==================================================

Giữ bell.

Unread badge:

small
burgundy/red semantic badge.

Hover bell:
light burgundy tint.

Nếu không có notification:
badge không hiển thị.

Không hard-code unread count.

==================================================
29. LINKS
==================================================

Không dùng default browser blue underline.

Normal app links:

neutral/burgundy according to context.

Hover:
burgundy darker
hoặc underline nhẹ nếu semantic link.

==================================================
30. TRANSITIONS
==================================================

Global guideline:

120–200ms.

Preferred:
150ms / 180ms.

Properties:

background-color
border-color
color
box-shadow
opacity
transform rất nhỏ nếu cần.

Không dùng:
500ms+
spring bounce
overshoot
continuous animation.

==================================================
31. ACCESSIBILITY
==================================================

Hover không được là feedback duy nhất.

Keyboard focus phải nhìn thấy.

Use:

:focus-visible

Focus ring rõ nhưng nhẹ.

Buttons phải có disabled state.

Disabled:
không hover như active control.

==================================================
32. REDUCED MOTION
==================================================

Nếu project CSS architecture cho phép:

respect:

@media (prefers-reduced-motion: reduce)

giảm/tắt transitions không cần thiết.

Không over-engineer.

==================================================
33. GLOBAL SEARCH
==================================================

Không mở rộng chức năng search trong task này.

Chỉ polish visual nếu cần.

Nếu global search hiện chỉ là visual placeholder:
KHÔNG fake kết quả.

Không sửa Backend để hỗ trợ search.

==================================================
34. ACTIVE NAVIGATION BUG
==================================================

Audit route matching.

Chỉ một sidebar item active tại một thời điểm.

Tránh prefix bug:

/questions
/questions/create

làm cả hai active.

Dùng exact/end hoặc matching logic phù hợp.

==================================================
35. HELP DUPLICATION
==================================================

Audit:

Trung tâm trợ giúp

Không render duplicate trong cùng navigation hierarchy.

Có thể tồn tại:

Sidebar bottom Help
và
Profile dropdown Help

điều này chấp nhận được vì là hai entry point.

Nhưng không có hai Help item trong sidebar.

==================================================
36. LOGOUT DUPLICATION
==================================================

Sidebar hiện có thể có Đăng xuất
và dropdown cũng có Đăng xuất.

Audit theo design source.

Ưu tiên chỉ một primary logout location nếu Stitch design yêu cầu.

Nếu giữ cả hai:
phải có lý do UX rõ ràng.

Không để navigation cảm giác lặp.

==================================================
37. RESPONSIVE
==================================================

Không phá responsive hiện tại.

Test:

desktop ~1920
laptop ~1366
tablet-ish width nếu Web hỗ trợ.

Dropdown không vượt viewport.

Error pages vẫn center hợp lý.

Sidebar không che content.

==================================================
38. DO NOT REDESIGN
==================================================

Đây là POLISH PASS.

Không redesign toàn Web.

Không đổi:

- layout architecture lớn
- HAU identity
- business workflow
- API hooks
- state management
- React framework

Chỉ:

routing correctness
role navigation
error pages
hover
micro-interactions
dropdown polish
consistency.

==================================================
39. DO NOT TOUCH BACKEND FOR 403
==================================================

Rất quan trọng.

Nếu USER click Ma trận đề và Backend trả 403:

KHÔNG sửa Backend để cho USER quyền.

Audit business permission.

Nếu USER không nên có quyền:
ẩn menu khỏi USER.

Frontend phải phản ánh quyền backend,
không mở rộng quyền backend để chiều theo menu sai.

==================================================
40. E2E REGRESSION
==================================================

Existing Playwright tests hiện có.

Sau changes:

run:

npm run lint
npm run build

và existing Playwright E2E.

Không sửa test chỉ để làm nó PASS nếu behavior test đang đúng.

Nếu navigation role thay đổi hợp lệ:
update E2E expectation tương ứng.

==================================================
41. ADD ERROR PAGE TESTS
==================================================

Nếu E2E architecture cho phép dễ dàng:

thêm lightweight tests:

Unknown URL
→ 404 visible.

Forbidden route for wrong role
→ 403 hoặc role redirect behavior đúng.

Error Boundary:
unit/component test nếu test infrastructure đã có.

Không tạo test framework mới chỉ vì task này.

==================================================
42. MANUAL ROLE CHECK
==================================================

Verify navigation config:

USER:
không thấy menu không có quyền.

SUBJECT_ADMIN:
chỉ thấy chức năng chuyên môn được phép.

SYSTEM_ADMIN:
chỉ thấy chức năng quản trị hệ thống phù hợp.

Không assume SYSTEM_ADMIN automatically owns all specialist UI.

==================================================
43. 403 DIRECT URL BEHAVIOR
==================================================

Dù menu đã bị ẩn:

USER vẫn có thể tự gõ URL.

Ví dụ:

/exam-matrix

Nếu role không được phép:

→ 403 page.

Điều này là đúng.

Menu hidden
+
direct unauthorized URL -> 403

là UX/security đúng.

==================================================
44. 404 DIRECT URL
==================================================

Ví dụ:

/abc-this-page-does-not-exist

→ 404

Không:
→ dashboard
→ blank page
→ 500.

==================================================
45. 500 / UNEXPECTED
==================================================

Unexpected render failure:

→ Error Boundary

API server failure:

Nếu ảnh hưởng toàn page:
friendly page-level error.

Nếu chỉ list:
inline retry.

Không logout user vì HTTP 500.

==================================================
46. ERROR TEXT
==================================================

User-facing text phải tiếng Việt.

Không:

Forbidden
Not Found
Internal Server Error
Something went wrong

trên UI chính.

Có thể giữ numeric:

403
404
500.

==================================================
47. DESIGN CONSISTENCY AUDIT
==================================================

Cuối task scan Web cho:

- cyan primary legacy
- blue default links
- inconsistent hover
- inconsistent border radius
- duplicate active menu
- duplicate identity
- raw enum
- English error text
- dead navigation
- menu visible nhưng role forbidden
- huge dropdown
- giant empty/error panels

Fix trong phạm vi Web.

==================================================
48. FILE BOUNDARY
==================================================

Before editing:

record changed-file scope.

After editing:

verify:

git diff --name-only

Tất cả changed files từ task này phải thuộc Web frontend.

Nếu có backend file:
REVERT phần thay đổi của task đó.

Không revert unrelated pre-existing user work.

==================================================
49. ACCEPTANCE CRITERIA
==================================================

Chỉ báo WEB_POLISH_READY nếu:

[ ] USER navigation không còn item sai quyền
[ ] Exam Matrix ownership đã audit
[ ] Exam Versions ownership đã audit
[ ] menu ↔ route ↔ guard thống nhất
[ ] direct unauthorized URL -> 403
[ ] unknown URL -> 404
[ ] 500 UI tồn tại
[ ] global Error Boundary tồn tại hoặc existing equivalent verified
[ ] 403 UI polish
[ ] 404 UI polish
[ ] 500 UI polish
[ ] error UI tiếng Việt
[ ] dropdown identity không lặp
[ ] hover sidebar nhẹ
[ ] hover button nhẹ
[ ] hover interactive card/row nhẹ
[ ] focus-visible rõ
[ ] không animation màu mè
[ ] không gradient
[ ] HAU burgundy giữ nguyên
[ ] no backend changes
[ ] no mobile changes
[ ] lint PASS
[ ] build PASS
[ ] existing Playwright PASS

==================================================
50. FINAL REPORT
==================================================

Trả report:

A. Navigation/permission problems found

B. Exam Matrix ownership conclusion

C. Exam Versions ownership conclusion

D. USER navigation after fix

E. SUBJECT_ADMIN navigation after fix

F. SYSTEM_ADMIN navigation after fix

G. 401 handling

H. 403 page

I. 404 page

J. 500/Error Boundary

K. Hover/micro-interactions

L. User dropdown changes

M. Accessibility/focus states

N. Files changed

O. Tests:
- lint
- build
- Playwright

P. Backend gaps found but NOT changed

Q. Final verdict:

WEB_POLISH_READY
or
WEB_POLISH_NOT_READY

Cuối cùng bắt buộc xác nhận:

Backend files changed: NONE
Mobile files changed: NONE
Docker: NOT RUN
.env secrets: NOT MODIFIED
commit: NONE
push: NONE